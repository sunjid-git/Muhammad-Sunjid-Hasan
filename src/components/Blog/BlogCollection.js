import React from 'react'

const BlogCollection = () => {
    return (
        <div>
            BlogCollection

               {/* threeBlog.map(blog=> <BlogThree key={blog.id} blog={blog.blog}></BlogThree>) */}
        </div>
    )
}

export default BlogCollection
